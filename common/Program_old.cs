using System;
using System.Collections.Generic;
/*
int a = 5, b = 10;

int max = (a > b) ? a : b;
Console.WriteLine(max);



// Step 1 
public class Employee
{
  public void DetermineWeeklySalary(int weeklyHours, int wage)
  {
    var salary = 40 * wage;
    Console.WriteLine("\nThis EMPLOYEE worked {0} hrs. " + "Paid for 40 hrs (no overtime) at $ {1}" +
                          "/hr = ${2} \n", weeklyHours, wage, salary);
    Console.WriteLine("---------------------------------------------\n");
  }
}

// Step 2 

public class Contractor : Employee
{
  public void DetermineWeeklySalary(int weeklyHours, int wage)
  {
    var salary = weeklyHours * wage;
    Console.WriteLine("\nThis CONTRACTOR worked {0} hrs. " + "Paid for {0} hrs (w/ overtime) at $ {1}" +
                      "/hr = ${2} ", weeklyHours, wage, salary);
  }
}

// Step 3 
class Program
{
  private static void Main(string[] args)
  {
    const int hours = 50, wage = 70;
    Employee person = new Contractor();
    person.DetermineWeeklySalary(hours, wage);
  }
}
*/